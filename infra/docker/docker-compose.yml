services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: arete
      POSTGRES_USER: arete
      POSTGRES_PASSWORD: arete_password
    ports:
      - "5432:5432"
    volumes:
      - arete_postgres:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: arete
      MINIO_ROOT_PASSWORD: arete_password
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - arete_minio:/data

volumes:
  arete_postgres:
  arete_minio:
